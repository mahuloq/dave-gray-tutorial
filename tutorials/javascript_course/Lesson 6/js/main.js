const myName = "Matthew Tucker";
let nameLength = 0;
console.log(myName);

nameLength = myName.length + 1;
console.log(nameLength);
console.log(myName.charAt(Math.floor(Math.random() * nameLength)));
